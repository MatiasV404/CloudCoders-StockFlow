rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Colección de usuarios - Acceso permisivo para desarrollo
    match /users/{userId} {
      // Cualquier usuario autenticado puede leer perfiles
      // (necesario para que operarios verifiquen códigos de proyecto)
      allow read: if request.auth != null;
      
      // Solo el dueño puede modificar su perfil
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Colección de productos - Acceso permisivo para desarrollo
    match /users/{userId}/products/{productId} {
      // Permitir lectura y escritura a cualquier usuario autenticado
      allow read, write: if request.auth != null;
    }
  }
}